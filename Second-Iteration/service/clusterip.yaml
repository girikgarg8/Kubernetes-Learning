apiVersion: v1
kind: Service
metadata:
  name: my-nginx-service
spec:
  selector:
    app: nginx-deployment # service now knows it has to load balance among pods which have label 'app: nginx-deployment'
  ports:
    - protocol: TCP
      port: 8080 # service port
      targetPort: 80 # pod port
  type: ClusterIP